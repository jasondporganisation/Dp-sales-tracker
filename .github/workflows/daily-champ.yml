name: Daily & Weekly Champ

on:
  repository_dispatch:
    types: [daily-champ]
  schedule:
    - cron: '50 11 * * *'   # 2 hrs early to offset GitHub Actions delay
  workflow_dispatch:

jobs:
  send-champ:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Send champ messages
        run: node scripts/send-champ.js
